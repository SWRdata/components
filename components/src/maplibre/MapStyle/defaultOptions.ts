import type { StyleOptions } from './types';

const opts: StyleOptions = {
	enableBuildingExtrusions: false,
	places: {
		showLabels: true
	},
	admin: {
		show: true,
		showLabels: true
	}
};

export default opts;
