import { Controls, Meta } from '@storybook/addon-docs/blocks';

import * as ScrollerStories from './Scroller.stories.svelte';

<Meta of={ScrollerStories} />

# Scroller

A scroller component for Svelte apps, based on svelte-scroller by Rich Harris (license) but using a different approach to positioning (`position: sticky`).

## Properties

<Controls of={ScrollerStories.Default} />
