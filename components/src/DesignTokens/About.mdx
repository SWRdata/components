import { Story, ColorPalette, ColorItem, Meta } from '@storybook/blocks';
import {shades, scales} from "./Colors"
import DesignTokens from "./DesignTokens.svelte"

<Meta title='Design Tokens'/>

<DesignTokens/>

# Design Tokens

## Shades

<ColorPalette>
{["violet", "red", "teal", "yellow", "forest", "pink", "orange", "plum", "apple", "blue", "gray"].map((el, i) => {
	return(
		<ColorItem
		title={`${el.split("")[0].toUpperCase()}${el.slice(1)}`}
		colors={shades[el]}
	/>
			) 
})}
</ColorPalette>

## Scales

### Linear
### Categorical

### Divergent

<ColorPalette>
{["red_blue", "red_violet",  "red_forest", "red_apple", "red_teal", "violet_teal","violet_orange", "blue_pink", "teal_pink","forest_plum","apple_plum",  "orange_blue", "orange_teal"].map((el, i) => {
	return(
		<ColorItem
		
		title={`${el.split("_").join("/")}`}
		subtitle={el}
		colors={scales[el]}
	/>
			) 
})}
</ColorPalette>